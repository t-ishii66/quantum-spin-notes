# ARGUE notes

## 論点: 測定演算子 M の定義

### 悩み
- いったん
  - `M |Yay> = Yay |Yay>`
  - `M |Oops> = Oops |Oops>`
  と書くと、「M の固有状態が |Yay>, |Oops>」という見方は自然に見える。
- しかし Alice の実験では、測定行為は単なる「叩く」ではなく「どこを叩くか」を含む操作。
- 叩く場所が変わると測定内容が変わるため、同じ `M` で表すと混乱する。

### 整理
- 測定は場所（方向）ごとに別演算子として書くべき:
  - `M_n`: 方向 `n` を叩く測定
- このとき固有状態は
  - `M_n |Yay_n> = (+1) |Yay_n>`
  - `M_n |Oops_n> = (-1) |Oops_n>`
- ここで `+1/-1` は声ラベル `Yay/Oops` に対応する数値。

### 180度反対側を叩く操作
- `-n` 方向で叩く測定は別演算子 `M_-n`。
- 関係は
  - `M_-n = - M_n`
  - `|Yay_-n> = |Oops_n>`
  - `|Oops_-n> = |Yay_n>`
- したがって「反対側を叩くと声が反転する」が自然に表現できる。

## 重要な結論
- 「1つの M に対して固有状態が2つ」ではなく、
  - 「各方向ごとに測定演算子 `M_n` があり、各 `M_n` に2つの固有状態がある」
  と理解するのが正しい。

## 本文への反映案（短文）
- 「本書で測定は `M` ではなく `M_n` と書く。添字 `n` は“どこを叩くか”を表す。」
- 「同じ `n` で見れば `|Yay_n>`,`|Oops_n>` は `M_n` の固有状態だが、叩く場所を変えると測定演算子そのものが変わる。」

## 補足: `M_z` と `M_-z` の混乱ポイント

### 固有値方程式の基本
- 例えば
  - `M_z |Yay_z> = (+1)|Yay_z>`
  - `M_z |Oops_z> = (-1)|Oops_z>`
- 固有値方程式では、右辺は同じベクトルのスカラー倍であり、別の状態ベクトルに「変わる」式ではない。

### `M_-z` の作用
- 測定軸を反転すると演算子は
  - `M_-z = - M_z`
  と書ける。
- したがって
  - `M_-z |Yay_z> = -|Yay_z>`
  - `M_-z |Oops_z> = +|Oops_z>`
  となる。

### では「Yay/Oops が反転する」とは何か
- 反転は「状態が変わる」のではなく、「ラベルの対応が変わる」こと。
- 軸 `-z` を基準にした固有状態ラベルは
  - `|Yay_-z> = |Oops_z>`
  - `|Oops_-z> = |Yay_z>`
- これにより、同じ物理状態でも測定軸を反転すると結果ラベルが反転して見える。

## Chapter_1 物理チェック（修正候補）

1. `|Yay_n> = -|Oops_n>` は誤り  
   - `-|ψ>` は `|ψ>` と同一物理状態。  
   - `Yay/Oops` は同一測定の別結果なので直交状態として扱う必要がある。

2. 期待値式から確率を導出しているように見える順序  
   - `⟨φ|M|φ⟩ = a|α|^2 + b|β|^2` は Born則を前提にした結果。  
   - 論理順は「確率解釈（要請） -> 期待値式」。

3. `|α|^2` と `α^2` の記法混在  
   - 複素数拡張を見据えるなら `|α|^2, |β|^2` に統一。

4. Sceneの「逆向き」直観をPhysics Noteで断定している  
   - Sceneでは仮説として可。  
   - Note側では「厳密には直交」と明示して誤解を固定化しない。

5. 固有値 `a,b` の性質が未定義  
   - 最低限「実数」「`a ≠ b`」を明記。

6. Chapter 1に物理内容を詰め込みすぎる構成リスク  
   - 発見章としては情報密度が高い。  
   - 演算子・内積・一般状態・期待値の一部は後章へ逃がす余地あり。
