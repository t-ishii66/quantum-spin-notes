# BLOCH rough notes

## 目的
- ブロッホ球の式
  $$
  |\psi\rangle=\cos\frac{\theta}{2}|Yay\rangle+e^{i\phi}\sin\frac{\theta}{2}|Oops\rangle
  $$
  を、Alice/Bob/Charlie の実験から自然に導く。
- 特に「$\phi$ はどうやって見つかるのか」を物語として納得できる形にする。

## まず押さえる本質
- `z` 基底（`|Yay>`,`|Oops>`）での確率だけを見ると $\phi$ は消える。
  $$
  P(Oops|z)=\sin^2(\theta/2),\quad P(Yay|z)=\cos^2(\theta/2)
  $$
- 理由:
  $$
  |e^{i\phi}\sin(\theta/2)|^2=\sin^2(\theta/2)
  $$
  なので位相は絶対値二乗で消える。
- したがって、`z` だけ叩いている限り $\theta$ しか分からない。

## 物語上の転換点（重要）
- Alice たちは「まず1回叩く」ので、状態は固有状態へ射影される。
- ここで Bob が気づく:
  - 「それだと毎回、状態を潰してから読んでいる」
  - 「見えない情報（位相）があるなら、潰す前に別向きで読む必要がある」
- つまり測定プロトコルを変える必要がある。

## 実験プロトコル案（Alice達にできる形）
1. 準備手順 `Prep(r)` で同一状態を大量に作る（同じ $r$、同じ待ち時間）。
2. サンプル群を3つに分ける。
3. それぞれで最初の1回目を別軸で叩く:
   - 群A: `z` 軸
   - 群B: `x` 軸（真横その1）
   - 群C: `y` 軸（真横その2）
4. 各群の Yay/Oops 比率を統計で取る。

ポイント:
- 各個体で「最初の1回目だけ」を使う（2回目は射影後なので情報が崩れる）。
- これで状態トモグラフィーの最小版になる。

## 数学への接続（本文で使う最小式）
- 一般に
  $$
  |\psi\rangle=\cos\frac{\theta}{2}|Yay\rangle+e^{i\phi}\sin\frac{\theta}{2}|Oops\rangle
  $$
- 期待値（または Yay 確率）として:
  $$
  \langle \sigma_z\rangle=\cos\theta,\quad
  \langle \sigma_x\rangle=\sin\theta\cos\phi,\quad
  \langle \sigma_y\rangle=\sin\theta\sin\phi
  $$
  $$
  P(Yay|n)=\frac{1+\langle \sigma_n\rangle}{2}
  $$
- 逆算:
  $$
  \theta=\arccos(\langle \sigma_z\rangle),\quad
  \phi=\mathrm{atan2}(\langle \sigma_y\rangle,\langle \sigma_x\rangle)
  $$

## 物語への落とし込み（ラフ台詞）
- Alice:
  - 「同じ準備なのに、横から叩くと統計が違う」
- Bob:
  - 「`Yay/Oops` の割合だけじゃ足りない。振幅に“向き”がある」
- Charlie:
  - 「その向きは1つの角度で書ける。赤道の方位角、$\phi$ だ」

## 「回転」の描写整理（混乱防止）
- 言い回しを統一する:
  - 物語では「測定軸を回す」を主語にする（読者に分かりやすい）。
- 物理的には同値な見方:
  - 軸を回す（受動）
  - 状態を逆向きに回す（能動）
- 本文ではまず受動で書き、後でユニタリ回転の章で能動へ接続。

## 複素数導入の必然性
- 実数係数だけだと、`x` 軸情報（$\cos\phi$）までは表せても、`y` 軸情報（$\sin\phi$）を同時に自然に保持しづらい。
- $e^{i\phi}$ を導入すると、1つの位相で `x`,`y` の両方向のずれを統一して扱える。
- ここで「なぜ複素数か」に物語上の必然が出る。

## 章構成への統合案（下書き）
1. `z` だけの観測則（既存）
2. Bobの違和感: 同じ $\theta$ で説明できないデータ
3. 真横2方向（`x`,`y`）の統計測定
4. $\phi$ 導入と複素振幅
5. $(\theta,\phi)$ が球面座標になる -> ブロッホ球

## 書く時の注意
- 「$\phi$ は単発の個体からは読めない。統計量として推定する」を明記。
- $\theta=0,\pi$（極）では $\phi$ が実質未定義であることを脚注で触れる。
- 初学者向けに「全体位相は観測不能、相対位相のみ可観測」を短く入れる。

## すぐ使える短文（本文差し込み候補）
- 「`z` 方向の声だけを聞いている限り、位相は見えない。位相は、別の向きで叩いたときに初めて統計の偏りとして現れる。」
- 「私たちが測っていたのは“どれだけ混ざっているか”だけだった。これから測るのは“どう混ざっているか”だ。」
