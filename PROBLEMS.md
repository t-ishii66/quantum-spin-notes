# PHYSICS_NOTE 正しさチェック

対象: `PHYSICS_NOTE.md`, `PHYSICS_NOTE2.md`, `PHYSICS_NOTE3.md`, `PHYSICS_NOTE4.md`, `PHYSICS_NOTE5.md`

評価方針: 初心者向けの導入ノートとして、大学物理学科学部生の入門レベルで読んだときに誤解を生みそうな箇所を中心に確認した。厳密な数学的補足や高度な例外は、本文の目的を損なわない限り問題扱いしていない。

## 総評

全体の物理の流れ、パウリ行列、回転演算子、ブロッホ球、CHSH 不等式の主要な計算は概ね正しい。致命的な式の符号ミスや、結論を反転させるような誤りは見つからなかった。

ただし、いくつかの箇所で「導入としては分かりやすいが、言い切りが強くて誤解を招きうる」説明がある。また、1箇所だけ数式表示の明確な typo がある。

## 要修正

### 1. `PHYSICS_NOTE3.md:363` の確率式に LaTeX typo がある

該当箇所:

```math
P(+x) = \cos^2\frac{\pi/4}{} = \frac{1}{2}
```

問題:

`\frac{\pi/4}{}` は分母が空で、数式として不自然。意図は `\cos^2(\pi/4)` または `\cos^2\frac{\pi}{4}`。

修正案:

```math
P(+x) = \cos^2\frac{\pi}{4} = \frac{1}{2}
```

## 修正推奨

### 2. `PHYSICS_NOTE3.md:400` の「直交するベクトルは2つしかない」は不正確

該当箇所:

> 2次元ヒルベルト空間では直交するベクトルは2つしかない

問題:

2次元ヒルベルト空間で、ある状態に直交する「物理状態」は一つの射線に決まる、という意味なら正しい。しかし「ベクトル」は全体位相や複素数倍まで含めれば無数にある。初心者は「2次元空間には直交ベクトルが2本しか存在しない」と誤読しやすい。

修正案:

> 2次元ヒルベルト空間では、ある状態に直交する物理状態は全体位相を除いて一つに決まる。一方、ブロッホ球ではそれが対蹠点として表される。

### 3. `PHYSICS_NOTE4.md:774` の「ブロッホベクトル」は一般のスピンには使いにくい

該当箇所:

> 状態ベクトルとしての周期は半整数 $j$ で $4\pi$、整数 $j$ で $2\pi$ だが、ブロッホベクトル（測定統計）の周期はいずれも $2\pi$ である。

問題:

ブロッホ球・ブロッホベクトルは基本的に2準位系、特にこのノートではスピン 1/2 の純粋状態の表現として導入されている。一般の $j=1, 3/2, \ldots$ の状態空間は $2j+1$ 次元で、単純なブロッホ球上の1点では表せない。

物理的な趣旨、つまり「半整数スピンの $2\pi$ 回転は状態ベクトルに全体符号を与えるが、単独の測定確率は変わらない」は正しい。ただし一般スピンの説明で「ブロッホベクトル」と呼ぶのは避けた方がよい。

修正案:

> 状態ベクトルとしての周期は半整数 $j$ で $4\pi$、整数 $j$ で $2\pi$ である。ただし半整数 $j$ の $2\pi$ 回転で生じる $-1$ は全体位相なので、単独の測定確率は変わらない。スピン 1/2 の場合には、ブロッホベクトルの向きは $2\pi$ で元に戻る。

### 4. `PHYSICS_NOTE5.md:472` は「Bob の状態そのものが変わる」と言い切ると解釈依存に見える

該当箇所:

> Alice の選択によって Bob の粒子の状態そのものが変わる。

問題:

標準的な射影測定の計算として、「Alice の測定結果で条件づけた Bob の条件付き状態が変わる」と言うのは正しい。ただし「Bob の粒子の状態そのものが変わる」と言い切ると、Alice の選択が遠方の Bob に物理的作用を及ぼした、という強い実在論的・収縮解釈に読める。

本文では直後に「信号は送れない」と注意しており大きな破綻ではないが、ベル不等式の導入では解釈と計算を分けた方が安全。

修正案:

> Alice の測定結果で条件づけると、Bob に割り当てる条件付き状態が $\vert {-a}\rangle$ または $\vert {-a'}\rangle$ に変わる。

### 5. `PHYSICS_NOTE5.md:474` の「4つの相関が互いに独立に動ける」はやや言い過ぎ

該当箇所:

> 4つの相関が互いに独立に動ける余地が生まれる

問題:

量子力学でも4つの相関を完全に自由に選べるわけではない。実際、同じ文書の補足で Tsirelson 限界 $2\sqrt{2}$ を説明している通り、量子相関にも制約がある。ここで言いたいのは、局所隠れ変数のように「同じ $A,A',B,B'$ の同時値を共有する」制約を受けない、ということ。

修正案:

> 4つの相関は、局所隠れ変数で仮定される同時に定まった $A,A',B,B'$ の代数的制約には従わない。

## 注意書き程度でよいもの

### 6. `PHYSICS_NOTE.md:45` のシュテルン・ゲルラッハの力の向きは装置・符号規約に依存する

該当箇所:

> 磁気モーメントの $z$ 成分が正なら磁力線が密な上側へ引かれ、負なら疎な下側へ押し出される。

問題:

導入説明としては十分だが、実際の力は $\mathbf{F}=\nabla(\boldsymbol{\mu}\cdot\mathbf{B})$ で決まり、磁場勾配の向きや、電子スピンと磁気モーメントの符号関係にも依存する。ここでは「上を $+1$ と呼ぶ」という測定結果のラベル付けが主眼なので、物理的な上下と $+/-$ を絶対的に結びつけない方が安全。

修正案:

> どちらのスポットを $+1$ と呼ぶかは装置の向きと符号規約による。以下では上側を $+1$ と名付ける。

### 7. `PHYSICS_NOTE4.md:263-283` の「交換関係が十分」の説明は入門向けとしてはよいが、数学的には少し圧縮されている

問題:

「3つのエルミート演算子が角運動量交換関係を満たせば本物の回転を作る」という主張は、リー代数の表現を指数写像で回転群またはその被覆群の表現へ持ち上げる話である。導入ノートとしては現在の説明で十分だが、厳密には大域的な位相や $\mathrm{SU}(2)$ が $\mathrm{SO}(3)$ の二重被覆であることが関わる。

修正するなら、「局所的な回転」または「$\mathrm{SU}(2)$ の表現として」という一言を加えるとより安全。

## 問題なしと判断した主要計算

- `PHYSICS_NOTE.md`: $\sigma_x,\sigma_y,\sigma_z$ の導出、固有状態、交換関係の計算は整合している。
- `PHYSICS_NOTE2.md`: 微小回転、ユニタリ性、$U=\exp(-i\theta G/\hbar)$、$G=L$ の同定の符号は本文の能動回転・演算子変換規約内で一貫している。
- `PHYSICS_NOTE3.md`: 一般方向の固有状態、ブロッホベクトル、$P(+\mathbf{n})=\cos^2(\Theta/2)$ の導出は正しい。
- `PHYSICS_NOTE4.md`: $U(\theta,\mathbf{n})=\cos(\theta/2)I-i\sin(\theta/2)\mathbf{n}\cdot\boldsymbol{\sigma}$、$2\pi$ 回転で $-I$、$4\pi$ 回転で $I$ となる計算は正しい。
- `PHYSICS_NOTE5.md`: シングレット相関 $E(\mathbf{a},\mathbf{b})=-\cos\theta$、CHSH 不等式、指定角度で $|S|=2\sqrt{2}$ となる計算は正しい。
