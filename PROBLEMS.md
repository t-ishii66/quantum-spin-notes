# PROBLEMS.md — PHYSICS_NOTE5.md の物理学的評価

## 総評

`PHYSICS_NOTE5.md` は、物理初心者向けのベル不等式入門としてはかなり完成度が高い。2粒子状態、シングレット、相関

```math
E(\mathbf{a},\mathbf{b})=-\cos\theta
```

の導出、CHSH 不等式

```math
|S|\le 2
```

の代数的導出、量子力学による

```math
|S|=2\sqrt{2}
```

の計算は、入門文書として十分に正しい。

ただし、いくつかの箇所は初心者に誤解を与えやすい。特に「古典的な相関は角度に対して線形に変化する」という説明は物理的に誤りなので、修正が必要である。また、ベル不等式が排除するものを「局所性か実在性のどちらか」と言い切る部分は、入門的には許容されるが、少しだけ慎重な言い方にした方がよい。

---

## 問題 1: 「古典的相関は角度に対して線形に変化する」は誤り（L365-367）

**箇所:** 隠れた変数理論では、角度 $\theta$ は「どの事前決定値が選ばれるか」の割合を変えるだけで、相関 $E$ は $\theta$ に対して線形に変化する、と述べている。

**問題:** これは物理的に正しくない。局所隠れ変数模型でも、相関関数 $E(\theta)$ は一般に直線である必要はない。隠れ変数の分布や、測定方向に対する応答関数の作り方によって、角度依存性は非線形にもなりうる。

ベル不等式のポイントは、「古典的相関は直線で、量子相関は曲線だから破れる」ということではない。正しくは、

- 各試行で $A(\mathbf{a},\lambda), A(\mathbf{a}',\lambda), B(\mathbf{b},\lambda), B(\mathbf{b}',\lambda)$ が同時に値を持つ
- それらの値が $\pm 1$ である
- Alice 側の値は Bob 側の設定に依存せず、Bob 側の値は Alice 側の設定に依存しない

という代数的構造から、どんな角度依存を工夫しても CHSH 量は $|S|\le 2$ を超えられない、という点である。

**修正案:** L365-367 の「線形に変化する」説明を削除し、次のように置き換える。

> 古典的な局所隠れ変数理論でも、相関 $E(\theta)$ の形そのものは必ずしも直線とは限らない。しかし、4つの測定設定に対して同時に定まった値を平均する限り、CHSH の組み合わせは必ず $|S|\le2$ に収まる。量子力学の $-\cos\theta$ は、適切な4方向を選ぶとこの上限を超える。

---

## 問題 2: シングレットの「任意方向」の説明が実際には $xz$ 平面に限定されている（L118-234）

**箇所:** 「任意の方向」で完全反相関が成り立つと述べた後、$\mathbf{a}$ を $xz$ 平面内に置いて、$y$ 軸まわりの回転だけで証明している。

**問題:** 計算そのものは正しいが、示しているのは $xz$ 平面内の方向についてである。もちろんシングレットは本当に任意方向で回転不変だが、この本文の計算だけでは一般の方位角を持つ方向までは直接示していない。

入門書としては、$xz$ 平面に限定して計算を簡単にするのはよい。ただし「一般方向の完全な証明ではなく、代表例として $xz$ 平面で示している」ことを一文で断ると安全である。

**修正案:** L122 付近に次を補う。

> ここでは計算を簡単にするため、方向を $xz$ 平面内に取る。シングレットは本当は任意の空間回転に対して不変なので、一般の方向でも同じ結論になる。

---

## 問題 3: 「Alice の測定が Bob に物理的影響を与えるわけではない」は強すぎる（L383-385）

**箇所:** Alice が測定すると Bob の粒子が即座に決まるが、Alice の測定が Bob の粒子に物理的な影響を与えるわけではない、と述べている。

**問題:** ここはベル不等式の核心に近いので、表現に注意が必要である。標準的な量子力学では、Alice の測定後に Bob 側の条件付き状態を更新して記述する。しかし、その更新を「物理的な超光速信号」として使うことはできない。つまり確実に言えるのは「超光速通信はできない」「Bob 単独の統計は Alice の測定選択で変わらない」ということである。

「物理的な影響はない」と言い切ると、ベルの定理が示す非局所性の問題を弱く見せすぎる恐れがある。

**修正案:** 次のように弱める。

> Alice の測定で Bob 側の条件付き状態は更新される。しかし Bob 単独で見える統計は変わらず、これを使って光より速く信号を送ることはできない。

---

## 問題 4: EPR の仮定を「すべての方向の答えが事前に決まる」とだけ書くと少し狭い（L391-417）

**箇所:** EPR/隠れた変数の仮説として、各粒子がすべての方向に対する測定結果を事前に持つ、と説明している。

**問題:** CHSH 不等式の導出では、実際には本文で使う4つの設定

```math
\mathbf{a},\mathbf{a}',\mathbf{b},\mathbf{b}'
```

について値が定まっていれば十分である。「すべての方向」と書いても直感的には分かりやすいが、初心者には仮定が必要以上に強く見える可能性がある。

また、一般の局所隠れ変数理論は確率的応答を許すこともある。ただし、その確率性も隠れ変数 $\lambda$ に含めて拡張すれば、CHSH の導出では決定論的な値 $A=\pm1$, $B=\pm1$ として扱える。この補足があると、入門書としても誤解が少ない。

**修正案:** 「少なくとも実験で選ばれる4つの設定について、測らなかった場合の値も同時に定まっていると考える」と書く。余裕があれば「確率的な隠れ変数模型も、乱数の種まで $\lambda$ に含めれば同じ形にできる」と脚注的に補う。

---

## 問題 5: CHSH 不等式の前提に「測定設定の独立性」が明示されていない（L409-417, L497）

**箇所:** 相関を

```math
E(\mathbf{a}, \mathbf{b}) = \int A(\mathbf{a}, \lambda)B(\mathbf{b}, \lambda)\rho(\lambda)d\lambda
```

と書き、「局所性 + 実在性」だけから CHSH 不等式が出るとしている。

**問題:** 入門書として「局所性 + 実在性」とまとめるのはよくあるが、実際にはもう一つ大切な前提として、測定設定の選択が隠れ変数 $\lambda$ と統計的に独立であることが必要である。つまり $\rho(\lambda)$ が Alice と Bob の設定 $\mathbf{a},\mathbf{b}$ に依存しない、という仮定である。

これは「自由選択」「測定独立性」「統計的独立性」などと呼ばれる。これを省くと、超決定論的な逃げ道を暗黙に除外していることが見えなくなる。

**修正案:** L415 付近に次を補う。

> ここでは、測定方向の選択は粒子が持つ $\lambda$ と独立であり、$\rho(\lambda)$ は Alice と Bob がどの方向を選ぶかに依存しないと仮定する。

L497 の「局所性 + 実在性だけ」は、「局所性 + 事前値 + 測定設定の独立性」または「標準的な局所実在論の仮定」に弱めるとよい。

---

## 問題 6: 「実在性か局所性の一方が誤り」と二分法にしすぎている（L572-581）

**箇所:** CHSH の破れから、少なくとも「実在性」または「局所性」の一方は誤りである、と結論している。

**問題:** 入門書としては分かりやすいまとめだが、少しだけ単純化しすぎである。ベル実験が否定するのは、より正確には次の組み合わせである。

- 測定しなかった設定にも結果が同時に定まっている
- Alice 側の結果は Bob 側の設定に依存せず、Bob 側の結果は Alice 側の設定に依存しない
- 測定設定の選択は $\lambda$ と独立である

したがって「局所性か実在性のどちらか」とだけ書くと、測定独立性や、確率的隠れ変数模型の扱いが見えなくなる。

**修正案:** 初心者向けには次の程度がよい。

> したがって、測定結果が事前に同時に決まっており、しかも遠方の測定設定に影響されない、という素朴な局所実在論は成り立たない。

その後に「よく短く言えば、局所性と実在性を同時には保てない」と添えると、分かりやすさも残せる。

---

## 問題 7: 「実験で $|S|=2\sqrt{2}$ に一致」は理想化が強い（L583-585）

**箇所:** 実験で量子力学の予測 $|S|=2\sqrt{2}$ に一致する結果が得られている、と述べている。

**問題:** 理想的な最大もつれ状態と最適な測定角では量子力学の最大値は $2\sqrt{2}$ である。しかし実際の実験では、ノイズ、検出効率、状態生成の不完全性などにより、測定値は $2\sqrt{2}$ ぴったりではなく、それに近い値、または明確に $2$ を超える値として観測される。

入門書としては大きな問題ではないが、「一致する」と言い切るより「近い値」「CHSH 上限を明確に超える」とした方が正確である。

**修正案:** 次のように修正する。

> 実験では、理想値 $2\sqrt{2}$ に近い相関、あるいは少なくとも古典上限 $2$ を明確に超える相関が観測されている。

---

## 問題 8: 「Tsirelson の限界」の説明はよいが、$4$ は「数学的上限」より「代数的上限」がよい（L589-605）

**箇所:** $2$, $2\sqrt{2}$, $4$ を「古典上限」「量子上限」「数学的上限」として並べている。

**問題:** 内容はほぼ正しい。ただし $4$ は、CHSH 式だけを見て相関値を $[-1,1]$ の範囲で自由に選んだ場合の最大値であり、通常は「代数的上限」と呼ぶ方が正確である。「数学的上限」でも初心者には伝わるが、少し曖昧である。

また、$4$ に達する相関は量子力学では不可能だが、非シグナリングな仮想相関（PR box）では可能、という文脈がある。入門書では説明しなくてよいが、「代数的上限」と呼ぶだけで十分正確になる。

**修正案:** 「数学的上限」を「代数的上限」に変更する。

---

## 問題 9: 「この文書の道具だけで進む」は少しだけ言い過ぎ（L5-15）

**箇所:** 新たに導入するのは「2粒子系の状態をどう書くか」だけで、それ以外は既知の道具の組み合わせで進む、と述べている。

**問題:** 本文では、テンソル積、もつれ状態、2粒子測定、条件付き状態、隠れ変数、局所性、測定独立性、CHSH 相関など、かなり新しい概念を導入している。もちろん入門書として順に説明しているので問題はないが、「新たに導入するのは2粒子状態だけ」と言うと、後半の概念的ジャンプを軽く見せすぎる。

**修正案:** 「新しい数学道具としては2粒子系のテンソル積を導入し、概念としては局所隠れ変数と CHSH の考え方を追加する」と書くとよい。

---

## 問題 10: 重大ではないが、偏光実験に触れるならスピンとの違いに注意（L583-585）

**箇所:** 光子の偏光や原子のスピンを使った実験で検証されている、と述べている。

**問題:** 光子偏光のベル実験は非常に重要で、入門書で触れるのはよい。ただし、本文の導出はスピン $1/2$ のシングレットと相関 $-\mathbf{a}\cdot\mathbf{b}$ に基づいている。光子偏光では角度依存が偏光角の二倍で現れるなど、測定量の対応が少し異なる。

初心者向けには詳細説明は不要だが、「光子偏光でも対応する形のベル実験が行われている」と少しぼかす方が安全である。

**修正案:** 「光子の偏光や原子・電子のスピンなど、対応する量子二値測定を使った実験で確認されている」とする。
