# STORY2: Aliceたちが `z` 測定を式にするまで

## 目的

この文書は、黒い球について Alice, Bob, Charlie が観測を重ねながら、

- `z` 軸方向の測定とは何か
- なぜ二つの確定状態を導入するのか
- なぜその測定が行列で書けるのか

を、物語として無理なくつなぐための下書きである。

ここではまだ `x`,`y` やブロッホ球までは進まない。  
まずは一つの軸に対する二値測定を、どう式にするかに集中する。

---

## Scene 2-1: 声が出る叩き方

放課後、Alice は黒い球を机の上で転がしながら、指先であちこちをつついていた。

「これ、触るだけだと静かなんだよね」

Bob が顔を上げる。  
「前に声が出たのは、もっとちゃんと叩いたときだったろ」

Alice は球のてっぺんを軽く叩いた。何も起こらない。  
次に真下の点を指で押す。やはり何も起こらない。

Charlie が球を持ち上げて、しばらく眺めた。  
「一つの点じゃなくて、向かい合う二つを同時に叩くとどうなる？」

Alice は目を丸くした。  
「え、そんな変な叩き方ある？」

三人は球の上下をはさむようにして、同時に小さく叩いた。

**Yay!**

Alice が飛び上がる。  
「出た！ しかも今の、上だけでも下だけでもなかった」

Bob がうなずく。  
「つまり球は、一点に反応してるんじゃない。一本の向きを選ばれたときに答えてる」

Charlie はノートに書いた。

- 測定とは、表面の一点を調べることではない
- 球の中心をはさんで向かい合う二点を同時に叩くことだ

「上下の二点を同時に叩くなら、これは `z` 軸方向の測定と呼ぶのが自然だ」

## Physics Note 2-1: 測定は「点」ではなく「軸」

この物語での測定は、球面上の一点を読む操作ではない。  
中心をはさんで正反対にある二点を同時に叩いて、一本の測定軸を選ぶ操作である。

したがって

- `(0,0,+1)` を叩く測定
- `(0,0,-1)` を叩く測定

が別々にあるのではない。  
あるのは、その二点が決める **`z` 軸方向の一つの測定** であり、返ってくる声がその軸に対する二値結果になっている。

この整理を最初に入れておくと、後で `+z` と `-z` を「別の測定」ではなく「同じ測定に対する二つの結果」として扱いやすい。

---

## Scene 2-2: 同じ軸で叩くと、声が固定される

Alice はもう一度、球の上下を同時に叩いた。

**Yay!**

「もう一回」

同じ場所を、同じように叩く。

**Yay!**

Bob が球を受け取る。  
「今度は俺」

Bob が同じ `z` 軸で叩く。

**Yay!**

Charlie が言う。  
「偶然じゃなさそうだ。少なくとも、一度この軸で答えたあと、その軸については状態が固定される」

Alice は今度は、球そのものには触らず、叩く二点の組のほうを少しずらした。

**Oops!**

さらに、同じ叩き方をもう一度する。

**Yay!**

Alice は腕を組んだ。  
「あ、今のはたまたま `Oops!` になっただけか。  
でも、さっきの上下の組で叩くときだけは、ずっと `Yay!` が続いてた」

Charlie が答える。  
「うん。だから“同じ軸なら結果が固定される”という事実と、“軸を変えると別の声が出ることがある”という事実は分けて考えたほうがいい」

Bob が補う。  
「まず確実なのは、`z` 軸については二つの確定した状態を考えるのが自然だ、ってことだな」

## Physics Note 2-2: 確定状態

同じ `z` 軸で繰り返し測ると、結果が再現される。  
この事実は、`z` 測定に対して二つの特別な状態を導入する動機になる。

それを

```math
|+z\rangle,\qquad |-z\rangle
```
と書く。

意味は次の通りである。

- 状態 $|+z\rangle$ なら、`z` 測定の結果は必ず `+`
- 状態 $|-z\rangle$ なら、`z` 測定の結果は必ず `-`

まだこの段階では、`+` や `-` は単なるラベルとして読めばよい。  
後で計算をする段階になったら、これを `+1,-1` と数値化する。

---

## Scene 2-3: Charlie が式を書き始める

Charlie はノートの新しいページを開いた。

「今わかっていることを、式としても書いておきたい」

Alice が少し顔をしかめる。  
「もう式なの？」

Bob が笑う。  
「でもここまで来ると、式がないと逆に整理できない」

Charlie はゆっくり書いた。

```math
|+z\rangle,\qquad |-z\rangle
```
「この二つは、同じ測定に対して違う答えを返す。だから完全に区別できないといけない」

Alice が言う。  
「“混ざってない”ってこと？」

「うん。量子力学では、それを直交していると表す」

Charlie は続けて書く。

```math
\langle +z|-z\rangle=0
```
さらに、

```math
\langle +z|+z\rangle=1,\qquad
\langle -z|-z\rangle=1
```
Bob がうなずく。  
「長さ 1 にそろえた、ってことだな」

Charlie は最後にまとめた。

```math
\langle +z|+z\rangle=1,\qquad
\langle -z|-z\rangle=1,\qquad
\langle +z|-z\rangle=0
```
Alice は少し黙ってから言った。  
「なんか急に本格的。でも、“二つの確定した声”をちゃんと分けて置いた感じはする」

## Physics Note 2-3: 直交規格化

$|+z\rangle$ と $|-z\rangle$ は、同じ測定に対して完全に区別可能な二つの状態である。  
量子力学では、この「完全に区別可能」を内積 0、つまり直交で表す。

```math
\langle +z|-z\rangle=0
```
また、それぞれを確率 1 の基準状態として扱うため、長さを 1 にそろえる。

```math
\langle +z|+z\rangle=1,\qquad
\langle -z|-z\rangle=1
```
このように、二つの状態を直交規格化基底として置くところから始める。

---

## Scene 2-4: 声を数で書く

Alice はノートをのぞきこんだ。

「でもまだ、“Yay” と “Oops” って言葉のままだよね」

Charlie が答える。  
「そう。ここから先は、結果を数にしたほうが計算しやすい」

Bob が口をはさむ。  
「たとえば `Yay` を `+1`、`Oops` を `-1` に対応させるわけだ」

Alice は少し考えてからうなずいた。  
「なるほど。声そのものを変えるんじゃなくて、記録の仕方を決めるんだ」

Charlie は式を書いた。

```math
\hat Z
=
(+1)|+z\rangle\langle +z|
+(-1)|-z\rangle\langle -z|
```
「これは `z` 軸測定そのものを表す演算子だ」

Alice が首をかしげる。  
「どういう意味？」

Charlie はノートを指差した。

- $|+z\rangle$ 成分には `+1` を返す
- $|-z\rangle$ 成分には `-1` を返す

「つまり、今まで声で聞いていた二値結果を、式で返す機械みたいなものだよ」

## Physics Note 2-4: 測定演算子

`z` 軸測定で返る二つの結果を `+1,-1` と数値化すると、測定演算子は

```math
\hat Z
=
(+1)|+z\rangle\langle +z|
+(-1)|-z\rangle\langle -z|
```
と書ける。

この式は、

- $|+z\rangle$ に対しては `+1`
- $|-z\rangle$ に対しては `-1`

を返すように作られている。

実際、

```math
\hat Z|+z\rangle=|+z\rangle,\qquad
\hat Z|-z\rangle=-|-z\rangle
```
となる。  
ここで右辺の `-` は、「状態がひっくり返った」という意味ではない。  
$|-z\rangle$ という状態に対して、測定値 `-1` が対応していることを表している。

この

```math
\hat Z|\psi\rangle=\lambda|\psi\rangle
```
という形は、

- $|\psi\rangle$ が固有状態
- $\lambda$ が固有値

であることを意味する。

---

## Scene 2-5: 行列の顔が見える

Charlie は最後に、基底を一つ固定しようと言った。

```math
|+z\rangle=
\begin{pmatrix}
1\\
0
\end{pmatrix},
\qquad
|-z\rangle=
\begin{pmatrix}
0\\
1
\end{pmatrix}
```
Bob が言う。  
「この順番で並べれば、$\hat Z$ はただの 2 × 2 行列として書けるな」

Charlie が計算すると、

```math
\hat Z=
\begin{pmatrix}
1&0\\
0&-1
\end{pmatrix}
```
となった。

Alice はしばらくその行列を見ていた。  
「これが、さっきまで球の声だったもの？」

Charlie はうなずいた。  
「そう。黒い球の `z` 軸測定を、いちばん短く正確に書いた顔がこれだ」

Bob が笑った。  
「名前もあるぞ。$\sigma_z$」

Alice はノートに太字で書いた。

**黒い球の声は、行列になる。**

## Physics Note 2-5: $\sigma_z$

$|+z\rangle, |-z\rangle$ を基底に選ぶと、

```math
\hat Z=
\begin{pmatrix}
1&0\\
0&-1
\end{pmatrix}
```
となる。  
これはそのままパウリ行列

```math
\sigma_z=
\begin{pmatrix}
1&0\\
0&-1
\end{pmatrix}
```
である。

したがって $\sigma_z$ は、最初から抽象的に与える必要はない。  
黒い球の `z` 軸方向の二値測定を、その確定状態とともに式へ書き下した結果として自然に現れる。

---

## この段階で手に入ったもの

1. 測定とは、向かい合う二点を同時に叩いて一本の軸を選ぶことだ  
2. `z` 軸測定には二つの確定状態 $|+z\rangle, |-z\rangle$ がある  
3. それらは直交規格化基底として置ける  
4. 測定結果を `+1,-1` に数値化すると、測定演算子 $\hat Z$ が定義できる  
5. その行列表現がパウリ行列 $\sigma_z$ になる
