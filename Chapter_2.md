# Chapter 2: The Rules of the Sphere

前章では、球を同じ軸で叩けば結果が再現することを確認した。この章では最も重要な問いに進む。**異なる軸**で叩いたら、何が起きるのか。

---

## 異なる軸の測定

三人は翌日、分度器を持ち出して実験を始めた。

手順はこうだ。まず $z$ 軸（上下）の対蹠2点を挟んで叩き、球の状態を確定させる。たとえば **Yay!**（$+1$）が出たとする。前章の再現性により、この直後に球は $\vert +z \rangle$ にある。

次に、 $z$ 軸から角度 $\theta$ だけ傾いた別の軸 $n$ を選び、その軸の対蹠2点を挟んで叩く。

結果は驚くべきものだった。同じ軸なら必ず同じ声が返ってくるはずなのに、別の軸では **Yay!** のときも **Oops!** のときもある。しかも、何回やっても同じ結果にはならず、ばらつく。

「壊れたんじゃないの？」と Bob が言ったが、Alice は首を振った。何十回、何百回と繰り返すうちに、ばらつきの中に規則が見えてきたからだ。

---

## 角度と確率

Alice が記録した統計を表にまとめると、次のようになった。

$z$ 軸で $+1$ が出た直後に、角度 $\theta$ だけ傾いた軸 $n$ で測定したときの結果:

<table border="1" cellspacing="0" cellpadding="6">
  <thead>
    <tr>
      <th>角度 θ</th>
      <th>n 軸で +1 が出る割合</th>
      <th>n 軸で −1 が出る割合</th>
    </tr>
  </thead>
  <tbody>
    <tr><td>0</td><td>1.00</td><td>0.00</td></tr>
    <tr><td>π/4</td><td>0.85</td><td>0.15</td></tr>
    <tr><td>π/2</td><td>0.50</td><td>0.50</td></tr>
    <tr><td>3π/4</td><td>0.15</td><td>0.85</td></tr>
    <tr><td>π</td><td>0.00</td><td>1.00</td></tr>
  </tbody>
</table>

$\theta = 0$ は同じ軸だから、前章の再現性どおり確率 $1$ で同じ声になる。 $\theta = \pi$ は反対向きだから、反転規則どおりである。問題はその中間で、確率が角度に応じて連続的に変化している。

Charlie がこの表に三角関数を当てはめた。

```math
P(+1) = \cos^2\frac{\theta}{2}, \qquad P(-1) = \sin^2\frac{\theta}{2}
```

検算してみよう。 $\theta = 0$ なら $\cos^2 0 = 1$ 。 $\theta = \pi$ なら $\cos^2(\pi/2) = 0$ 。 $\theta = \pi/2$ なら $\cos^2(\pi/4) = 1/2$ 。すべて表と一致する。さらに

```math
\cos^2\frac{\theta}{2} + \sin^2\frac{\theta}{2} = 1
```

だから確率の合計も常に $1$ になる。これが球の**角度則**である。

---

## なぜ θ/2 なのか

ここで自然な疑問が生じる。2つの軸のなす角は $\theta$ なのに、なぜ確率には $\theta/2$ が現れるのか。

この問いへの答えは第4章で得られる。球の状態を球面上の点として表すと、幾何学的な角度と状態空間の角度の間にちょうど2倍の関係があることが見える。今の段階では、 $\theta/2$ は実験事実が要求する形として受け入れておく。

---

## 重ね合わせ

角度則は、球の内部状態について重要なことを教えている。

$z$ 軸で $+1$ が確定した状態 $\vert +z \rangle$ の球を、別の軸 $n$ で測ると、$+1$ も $-1$ も出うる。つまり $\vert +z \rangle$ は $n$ 軸の測定に対しては結果が確定していない。それでいて確率はきちんと角度で決まっている。

この状況を記述するために、**重ね合わせ**（superposition）を導入する。 $\vert +z \rangle$ を、 $n$ 軸の固有状態で展開して

```math
\vert +z \rangle = \alpha \vert +n \rangle + \beta \vert -n \rangle
```

と書く。この式の意味は、「 $\vert +z \rangle$ は $\vert +n \rangle$ でも $\vert -n \rangle$ でもないが、両方の成分を持っている」ということである。

---

## 係数の決定

係数 $\alpha, \beta$ の値は、角度則から決められる。

ここで一つの仮定を置く。測定で $+1$ が出る確率は $\vert \alpha \vert^2$ 、 $-1$ が出る確率は $\vert \beta \vert^2$ である、と仮定する。この「**係数の絶対値の二乗が確率を与える**」という規則を**ボルン則**（Born rule）と呼ぶ。

なぜ「係数そのもの」ではなく「係数の二乗」なのか。素朴に考えれば、確率 $\cos^2(\theta/2)$ をそのまま係数にする候補もありえる。

```math
\text{候補A}: \quad \alpha = \cos^2\frac{\theta}{2}, \quad \beta = \sin^2\frac{\theta}{2}
```

```math
\text{候補B}: \quad \alpha = \cos\frac{\theta}{2}, \quad \beta = \sin\frac{\theta}{2}
```

候補Aは $\vert \alpha \vert^2 + \vert \beta \vert^2 = 1$ を満たさない（ $\cos^4 + \sin^4 \neq 1$ ）。一方、候補Bなら

```math
\vert \alpha \vert^2 + \vert \beta \vert^2 = \cos^2\frac{\theta}{2} + \sin^2\frac{\theta}{2} = 1
```

が自動的に成り立つ。確率の合計が $1$ であることと、係数のノルムの二乗の合計が $1$ であることが、候補Bでは同じ式になる。これは偶然ではなく、状態ベクトルの「長さ $1$ 」という条件と確率の規格化が一致する構造である。

したがって係数は候補Bを採用する。

```math
\vert +z \rangle = \cos\frac{\theta}{2}\,\vert +n \rangle + \sin\frac{\theta}{2}\,\vert -n \rangle
```

---

## 逆の展開

この関係を逆に読むと、 $n$ 軸の固有状態を $z$ 軸の固有状態で展開できる。 $z$ 軸と $n$ 軸のなす角が $\theta$ のとき

```math
\vert +n \rangle = \cos\frac{\theta}{2}\,\vert +z \rangle + \sin\frac{\theta}{2}\,\vert -z \rangle
```

```math
\vert -n \rangle = -\sin\frac{\theta}{2}\,\vert +z \rangle + \cos\frac{\theta}{2}\,\vert -z \rangle
```

2番目の式で $-\sin$ が現れる理由は、 $\vert +n \rangle$ と $\vert -n \rangle$ が直交しなければならないからだ。同じ軸の測定で $+1$ と $-1$ は排他的な結果だから、対応する状態が直交するのは当然の要請である。実際に内積を計算すると

```math
\cos\frac{\theta}{2}\Bigl(-\sin\frac{\theta}{2}\Bigr) + \sin\frac{\theta}{2}\cos\frac{\theta}{2} = 0
```

となり、直交が確認できる。

### 検算

球が $\vert +n \rangle$ にあるとき、 $z$ 軸で測定する。 $+1$ が出る確率は

```math
P(+1) = \cos^2\frac{\theta}{2}
```

$-1$ が出る確率は

```math
P(-1) = \sin^2\frac{\theta}{2}
```

角度則と一致する。球が $\vert -n \rangle$ にあるときは $P(+1) = \sin^2(\theta/2)$ となり、確率が入れ替わる。反転規則とも整合している。

---

## 測定後の状態更新

重ね合わせの枠組みで、もうひとつ重要なことが言える。

球が $\vert +z \rangle$ にあるとき、 $n$ 軸で測定して $+1$ が出たとする。その直後にもう一度 $n$ 軸で測ると、必ず $+1$ が出る。これは前章の再現性そのものだが、重ね合わせの言葉では次のように読める。

測定前:

```math
\vert +z \rangle = \cos\frac{\theta}{2}\,\vert +n \rangle + \sin\frac{\theta}{2}\,\vert -n \rangle
```

$n$ 軸測定で $+1$ が出た直後:

```math
\vert +n \rangle
```

$\vert -n \rangle$ の成分は消え、状態は $\vert +n \rangle$ に変わった。測定は状態を読み取るだけの操作ではない。測定は**状態を変える**。この事実を**状態更新**（state update）と呼ぶ。

ただし、状態更新の前後で変わらないものもある。更新後の $\vert +n \rangle$ に対して**同じ軸**でもう一度測れば、必ず $+1$ が出る。これが再現性である。状態更新は、測定した軸の固有状態への移行であり、その軸に関する情報を確定させる操作なのだ。

---

## 反平行の関係

前章の反転規則を、新しい記法で整理しておく。

軸 $n$ を反転した軸を $-n$ と書く。前章で確認したように、同じ軸を反対から挟めば声が反転する。これは

```math
\vert +(-n) \rangle = \vert -n \rangle, \qquad \vert -(-n) \rangle = \vert +n \rangle
```

と書ける。 $-n$ 方向で $+1$ が出る状態は、 $n$ 方向で $-1$ が出る状態と同じである。軸の向きを反転しても新しい物理は生まれない——ラベルが入れ替わるだけだ。

---

## 複素数への伏線

この章では係数 $\alpha, \beta$ を実数として扱った。実際、 $z$ 軸と $n$ 軸が同一平面内にある限り、実数の係数で角度則を完全に再現できる。

しかし球は三次元空間に存在する。測定軸は一つの平面に限られない。いま 2つの基底 $\vert +z \rangle, \vert -z \rangle$ で張られる空間は二次元だが、実数の係数2つでは「平面内の角度」という一つの自由度しか表せない。三次元空間の任意の軸方向を記述するにはもう一つの自由度が必要で、それが係数の**位相**——つまり複素数——として現れることになる。この問題は第4章で正面から扱う。

---

## 2次元空間の発見

この章の結果を振り返ると、重要な構造が見えてくる。

$z$ 軸の固有状態 $\vert +z \rangle, \vert -z \rangle$ は、他のあらゆる軸の固有状態を展開するための基底になっている。 $n$ 軸であれ $m$ 軸であれ、どの方向の固有状態も

```math
(\text{係数}_1)\,\vert +z \rangle + (\text{係数}_2)\,\vert -z \rangle
```

の形で書ける。つまり、球のすべての状態は**2つの基底で張られる空間**の中に住んでいる。

ならば、測定という操作もこの2次元空間の中の変換として書けるはずだ。2次元空間上の線形な変換は、$2 \times 2$ の行列で表される。次章では、測定を行列として書き下すことで、球の規則の背後にある数学的構造を明らかにする。

---

## この章で手に入れたもの

1. 異なる軸で測ると結果は確率的になる。確率は2つの軸のなす角 $\theta$ で決まる。
2. **角度則**: $P(+1) = \cos^2(\theta/2)$, $\quad P(-1) = \sin^2(\theta/2)$ 。
3. 状態の重ね合わせを導入した: $\vert +n \rangle = \cos(\theta/2)\,\vert +z \rangle + \sin(\theta/2)\,\vert -z \rangle$ 。
4. 係数の絶対値の二乗が確率を与える（**ボルン則**）。候補の比較から、規格化条件 $\vert \alpha \vert^2 + \vert \beta \vert^2 = 1$ と整合する候補Bを採用した。
5. 測定は**状態を更新する**。重ね合わせから一方の固有状態へ変化する。
6. 反平行の関係: $\vert +(-n) \rangle = \vert -n \rangle$ 。
7. すべての状態が $\vert +z \rangle, \vert -z \rangle$ の2つで展開できる——球の状態空間は**2次元**である。
8. 次章では、測定を $2 \times 2$ 行列として書く。

次章では、この2次元空間で測定がどんな行列になるかを、実験事実から導く。
